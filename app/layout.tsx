import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "IIT KGP MME Conference | Advanced Materials for Sustainable Energy",
  description: "Exploring Advanced Materials and Critical Minerals for Sustainable Energy Transformation - IIT Kharagpur Metallurgical & Materials Engineering Conference",
  keywords: "conference, materials engineering, metallurgical engineering, IIT Kharagpur, sustainable energy, advanced materials, critical minerals",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
